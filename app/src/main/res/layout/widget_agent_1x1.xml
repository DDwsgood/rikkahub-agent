<?xml version="1.0" encoding="utf-8"?>
<!-- 1x1 cell: app icon + one tiny line with the next scheduled-job time.
     RemoteViews-safe views only (LinearLayout / ImageView / TextView). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/widget_agent_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical"
  android:gravity="center"
  android:padding="2dp">

  <ImageView
    android:id="@+id/widget_agent_icon"
    android:layout_width="32dp"
    android:layout_height="32dp"
    android:src="@mipmap/ic_launcher"
    android:contentDescription="@string/app_name" />

  <TextView
    android:id="@+id/widget_agent_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:singleLine="true"
    android:ellipsize="end"
    android:textSize="9sp"
    android:text="@string/app_name" />
</LinearLayout>
